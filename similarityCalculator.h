#ifndef SIMILARITY_SCORE_H
#define SIMILARITY_SCORE_H

#include <vector>
#include <unordered_map>

long long calculateSimilarityScore(const std::vector<int>& left, const std::vector<int>& right);

#endif
